# 临时核对/自检文件（不进版本库）
_verify/
_ucl/
urls.txt
codes.txt
check-report.txt
check.js
check-hk.js
genverify.js
# _check41.py 每次跑都会重写这两个探针页（站外脚本，见该文件开头）
_probe.html
_nojs-probe.html

# 抓取/核对用的中间稿，不进版本库
*.tsv

# Word 打开文档时生成的属主临时文件（~$xxx.docx）。它同时会锁住原文件——
# build_docx.js 撞上时只跳过被锁的那一份并明确报出来，不会整个构建断在半路
~$*
